/*jslint node: true */
'use strict';

var lodash = require('lodash'),
    when = require('when'),
    fs = require('fs'),

ASave = {
    namer: function (I, D) {
    },
    array: function (L, cb, opts) {
    }
};

module.exports = ASave;
